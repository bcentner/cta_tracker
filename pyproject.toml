[tool.poetry]
name = "cta_tracker"
version = "0.0.1"
description="A tracker for CTA"
authors =["Benjamin Centner"]

[tool.poetry.dependencies]
python = "^3.11"
flask = "3.1.0"
xmltodict = "0.14.2"
requests = "2.32.3"
pydantic = "2.11.3"
gunicorn = "^23.0.0"

[build-system]
requires = ["poetry-core~=1.0"]
build-backend = "poetry.core.masonry.api"